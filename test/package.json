{
  "name": "finally-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "End-to-end Playwright tests for the FinAlly AI Trading Workstation",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "typescript": "^5.6.0"
  }
}
