---
phase: 01-backend-boot-streaming-hello-world
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pyproject.toml
  - backend/uv.lock
  - backend/app/main.py
  - backend/app/__init__.py
  - backend/app/api/__init__.py
  - backend/app/api/health.py
  - backend/tests/api/__init__.py
  - backend/tests/api/test_health.py
  - backend/tests/api/test_main.py
  - backend/CLAUDE.md
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - APP-06
must_haves:
  truths:
    - "Running `cd backend && uv sync --extra dev` succeeds from a clean checkout"
    - "Running `cd backend && uv run uvicorn app.main:app --port 8000` boots the server with no errors"
    - "`GET /api/health` returns 200 with JSON body matching `{status, source, tickers, cache_version}`"
    - "`GET /api/stream/prices` returns `Content-Type: text/event-stream` and yields a `data:` event within 2 seconds"
    - "On startup, the SimulatorDataSource is constructed with all 10 SEED_PRICES tickers and begins emitting price updates"
    - "On shutdown (SIGINT or lifespan exit), `await source.stop()` runs without raising"
  artifacts:
    - path: backend/app/main.py
      provides: "FastAPI app factory + lifespan + router mounts + static-files mount"
      contains: "lifespan"
      min_lines: 40
    - path: backend/app/api/__init__.py
      provides: "api_router that aggregates per-concern routers"
      contains: "api_router"
    - path: backend/app/api/health.py
      provides: "GET /api/health endpoint"
      contains: "def health"
    - path: backend/pyproject.toml
      provides: "Restored dependency manifest"
      contains: "fastapi"
    - path: backend/tests/api/test_health.py
      provides: "Unit test for /api/health payload shape and status code"
      contains: "def test_"
  key_links:
    - from: backend/app/main.py
      to: backend/app/market/factory.py
      via: "lifespan calls `create_market_data_source(cache)` and `await source.start(default_tickers)`"
      pattern: "create_market_data_source"
    - from: backend/app/main.py
      to: backend/app/market/stream.py
      via: "lifespan calls `app.include_router(create_stream_router(cache))`"
      pattern: "create_stream_router"
    - from: backend/app/main.py
      to: backend/app/api/__init__.py
      via: "`app.include_router(api_router, prefix=\"/api\")`"
      pattern: "api_router"
    - from: backend/app/api/health.py
      to: app.state
      via: "reads request.app.state.price_cache and request.app.state.market_source"
      pattern: "request\\.app\\.state"
---

<objective>
Stand up the FastAPI application root so the existing `backend/app/market/` subsystem is reachable over HTTP/SSE.

Purpose: Phase 1 success criteria 1, 2, 3, and 5 — Docker can build the backend, the container responds to `curl /api/health`, `curl /api/stream/prices` streams, and restart is clean.
Output: `backend/app/main.py` + `backend/app/api/` package + `/api/health` endpoint + tests + restored `pyproject.toml`.
</objective>

<execution_context>
@C:/Temp/projects/finally/.claude/get-shit-done/workflows/execute-plan.md
@C:/Temp/projects/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-boot-streaming-hello-world/01-CONTEXT.md
@.planning/phases/01-backend-boot-streaming-hello-world/01-SKELETON.md
@.planning/codebase/CONVENTIONS.md
@backend/app/market/__init__.py
@backend/app/market/interface.py
@backend/app/market/factory.py
@backend/app/market/stream.py
@backend/app/market/seed_prices.py
@backend/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restore backend/pyproject.toml from git HEAD</name>
  <files>backend/pyproject.toml, backend/uv.lock</files>
  <read_first>backend/uv.lock, .planning/codebase/STACK.md</read_first>
  <action>
The working tree currently has `D backend/pyproject.toml`. The HEAD version (`38b3398` and later) contains the locked dependency set (`fastapi>=0.115.0`, `uvicorn[standard]>=0.32.0`, `numpy>=2.0.0`, `massive>=1.0.0`, `rich>=13.0.0`, plus `[project.optional-dependencies].dev` with `pytest>=8.3.0`, `pytest-asyncio>=0.24.0`, `pytest-cov>=5.0.0`, `ruff>=0.7.0`). Restore it verbatim:

    git restore backend/pyproject.toml

Do NOT recreate from scratch — `uv.lock` (committed) is keyed to the HEAD pin set, so a recreated `pyproject.toml` risks lock drift. After restore, run `cd backend && uv sync --extra dev` to confirm dependencies install cleanly against the existing lockfile (this regenerates `.venv/` if absent — that's expected).

Do NOT modify `uv.lock` in this task. If `uv sync` requests a lock update because resolution drifted, stop and surface to the user — it likely means a pin was wrong upstream.
  </action>
  <verify>cd backend && test -f pyproject.toml && uv sync --extra dev && uv run python -c "import fastapi, uvicorn, numpy, massive"</verify>
  <acceptance_criteria>
    - `backend/pyproject.toml` exists at HEAD path
    - `grep -E '^fastapi\\s*=|^\"fastapi\\b' backend/pyproject.toml` shows the FastAPI pin
    - `cd backend && uv sync --extra dev` exits 0
    - `cd backend && uv run ruff check app/` exits 0
    - `cd backend && uv run pytest tests/market/ -q` exits 0 (existing market tests still pass)
    - `git status backend/pyproject.toml` no longer shows `D`
  </acceptance_criteria>
  <done>pyproject.toml is back; existing test suite green against current uv.lock.</done>
</task>

<task type="auto">
  <name>Task 2: Create `backend/app/api/` package with /api/health</name>
  <files>backend/app/api/__init__.py, backend/app/api/health.py, backend/tests/api/__init__.py, backend/tests/api/test_health.py</files>
  <read_first>backend/app/market/__init__.py, backend/app/market/factory.py, backend/app/market/stream.py, backend/app/__init__.py, backend/tests/conftest.py, .planning/codebase/CONVENTIONS.md</read_first>
  <action>
Create `backend/app/api/__init__.py` that builds an `api_router = APIRouter()` and `include_router`s each child router. Export `api_router` and a helper `def build_api_router() -> APIRouter` (so tests can mount without side effects). For Phase 1, the only child is health.

Create `backend/app/api/health.py`:
- `router = APIRouter()` and `@router.get("/health")`
- Async function `async def health(request: Request) -> dict` that reads `request.app.state.price_cache` and `request.app.state.market_source`.
- Return shape: `{"status": "ok", "source": <label>, "tickers": <int>, "cache_version": <int>}`
- `<label>` is derived from `type(request.app.state.market_source).__name__`: map `SimulatorDataSource` → `"simulator"`, `MassiveDataSource` → `"massive"`. Any other class → `type(...).__name__.lower().removesuffix("datasource")`.
- `<int>` for tickers = `len(request.app.state.price_cache.get_all())`. For cache_version = `request.app.state.price_cache.version`.
- Module conventions: `from __future__ import annotations` first; PEP 604 unions only; `logger = logging.getLogger(__name__)` (no logging calls required in Phase 1, but the logger handle is conventional).

Create `backend/tests/api/__init__.py` (empty, like other `tests/` packages).

Create `backend/tests/api/test_health.py` with two tests:
1. `test_health_returns_expected_payload_shape` — uses `httpx.AsyncClient` against an in-memory `FastAPI()` app built with the lifespan; asserts 200 + JSON shape (`status`, `source`, `tickers`, `cache_version`); asserts `tickers == 10` after lifespan startup; asserts `cache_version >= 0`.
2. `test_health_source_label_matches_no_env` — without `MASSIVE_API_KEY` set in `monkeypatch`, asserts `source == "simulator"`.

Use `pytest.mark.asyncio` (covered by `asyncio_mode = "auto"`). For the test client, use `httpx.AsyncClient(transport=httpx.ASGITransport(app=app), base_url="http://testserver")` so lifespan runs (`async with app.router.lifespan_context(app):` or `LifespanManager` from `asgi-lifespan` — the latter requires a new dev dep, so prefer manual `lifespan_context` invocation).

Note: `httpx` is already a transitive dep of `fastapi` via `uvicorn[standard]`. Verify by `uv run python -c "import httpx"`; if missing, add `httpx>=0.27` under `[project.optional-dependencies].dev` and re-run `uv sync --extra dev` (this is the one allowed edit to `pyproject.toml` for this plan, since httpx is a test-only need).
  </action>
  <verify>cd backend && uv run pytest tests/api/test_health.py -q</verify>
  <acceptance_criteria>
    - `backend/app/api/__init__.py` exports `api_router` (importable)
    - `backend/app/api/health.py` defines `router` with a `GET /health` route
    - `grep -nE "request\\.app\\.state\\.(price_cache|market_source)" backend/app/api/health.py` shows both reads
    - `backend/tests/api/test_health.py` has at least 2 test functions starting with `def test_`
    - `cd backend && uv run pytest tests/api/test_health.py -q` exits 0 with ≥2 tests passed
    - `cd backend && uv run ruff check app/ tests/` exits 0
  </acceptance_criteria>
  <done>/api/health works against an in-memory app; tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Wire backend/app/main.py with lifespan + router mounts</name>
  <files>backend/app/main.py, backend/app/__init__.py, backend/tests/api/test_main.py</files>
  <read_first>backend/app/market/interface.py, backend/app/market/factory.py, backend/app/market/stream.py, backend/app/market/seed_prices.py, backend/app/api/__init__.py, .planning/codebase/CONVENTIONS.md, .planning/codebase/ARCHITECTURE.md</read_first>
  <action>
Create `backend/app/main.py`:

```
from __future__ import annotations

import logging
from contextlib import asynccontextmanager
from collections.abc import AsyncIterator

from fastapi import FastAPI

from app.api import api_router
from app.market import PriceCache, create_market_data_source, create_stream_router
from app.market.seed_prices import SEED_PRICES

logger = logging.getLogger(__name__)


@asynccontextmanager
async def lifespan(app: FastAPI) -> AsyncIterator[None]:
    # construct singletons
    cache = PriceCache()
    source = create_market_data_source(cache)
    default_tickers = list(SEED_PRICES.keys())

    # mount SSE router (existing market subsystem; argument-injected)
    app.include_router(create_stream_router(cache))

    # publish to app.state for routes
    app.state.price_cache = cache
    app.state.market_source = source

    await source.start(default_tickers)
    logger.info("FinAlly backend started with %d tickers via %s", len(default_tickers), type(source).__name__)
    try:
        yield
    finally:
        await source.stop()
        logger.info("FinAlly backend stopped cleanly")


def create_app() -> FastAPI:
    app = FastAPI(title="FinAlly", lifespan=lifespan)
    app.include_router(api_router, prefix="/api")
    # Static-files mount is added in Plan 01-03 (Dockerfile slice) — Phase 1 backend-only run does not need it.
    return app


app = create_app()
```

Rationale for ordering inside lifespan:
- Cache + source constructed BEFORE the SSE router is mounted (router needs the cache reference).
- `app.state` is populated AFTER the router is mounted but BEFORE `source.start()`, so any route accessed in the brief window between mount and start sees an empty cache (acceptable — `start()` is fast and `/api/stream/prices` will just send the initial `retry: 1000` chunk).
- `await source.stop()` is in a `finally` block to honor the lifecycle contract (`MarketDataSource.stop` is idempotent — safe under any shutdown path).

Update `backend/app/__init__.py` to a minimal module docstring like `"""FinAlly backend package."""` if it is not already.

Create `backend/tests/api/test_main.py`:
1. `test_create_app_returns_fastapi_instance` — sync, calls `create_app()` and asserts the result is a `FastAPI` instance; asserts at least one route with path `/api/health` exists in `app.routes`.
2. `test_lifespan_starts_and_stops_source` — async, builds `app = create_app()`; uses `app.router.lifespan_context(app)` as an async ctx manager; inside it, asserts `app.state.price_cache` is a `PriceCache`, `app.state.market_source` exists, and `len(app.state.price_cache.get_all()) == 10` after a brief `await asyncio.sleep(0.6)` (one simulator tick at the default 0.5 s interval, with margin); after exiting the context, asserts `app.state.market_source._task is None` (the source has been stopped — per simulator.py:232 contract).
3. `test_lifespan_uses_seed_prices_default_tickers` — async, builds `app = create_app()`, enters lifespan, asserts the keys present in `app.state.price_cache.get_all()` after one tick is a superset of `set(SEED_PRICES.keys())` (cache may have more if test order is unlucky; superset is safe).

`MASSIVE_API_KEY` MUST be unset for these tests (use `monkeypatch.delenv("MASSIVE_API_KEY", raising=False)`) so the factory selects `SimulatorDataSource`.
  </action>
  <verify>cd backend && uv run pytest tests/api/ -q && uv run python -c "from app.main import app; print(app.title)"</verify>
  <acceptance_criteria>
    - `backend/app/main.py` defines `app = FastAPI(...)` and `def create_app() -> FastAPI`
    - `grep -n 'create_stream_router' backend/app/main.py` shows the mount call inside lifespan
    - `grep -n 'app.include_router(api_router' backend/app/main.py` shows the api_router mount with `prefix="/api"`
    - `grep -n 'SEED_PRICES' backend/app/main.py` shows the default-tickers source
    - `grep -n 'from __future__ import annotations' backend/app/main.py` shows the convention import
    - `cd backend && uv run pytest tests/api/ -q` exits 0 with ≥5 tests passed (2 from test_health + 3 from test_main)
    - `cd backend && uv run pytest tests/market/ -q` still exits 0 (no regression in the validated slice)
    - `cd backend && uv run python -c "from app.main import app; assert app.title == 'FinAlly'"` exits 0
    - `cd backend && uv run ruff check app/ tests/` exits 0
  </acceptance_criteria>
  <done>`uvicorn app.main:app` can boot; lifespan wires cache + source + SSE + api_router; tests cover all three.</done>
</task>

<task type="auto">
  <name>Task 4: Update backend/CLAUDE.md to remove the market_data_demo reference</name>
  <files>backend/CLAUDE.md</files>
  <read_first>backend/CLAUDE.md</read_first>
  <action>
Delete the `## Demo` section (the heading and its `uv run market_data_demo.py` code block at the bottom of the file). The demo file is staged for deletion as part of the GSD reset and will not be restored (per CONTEXT.md Claude's Discretion). Replace nothing — the section just disappears.

Add a new short `## Running the App` section at the bottom in its place:

```
## Running the App

For local development without Docker:

    uv run uvicorn app.main:app --reload --port 8000

Then open http://localhost:8000/api/health to verify the backend is up. The Next.js frontend (`frontend/`) is built and served via the Dockerfile in production; for frontend-only iteration run `npm run dev` inside `frontend/` against the running backend on :8000.
```

Do not touch the rest of `backend/CLAUDE.md`. The Market Data API, Core Types, SSE Streaming, Seed Data, and Running Tests sections stay intact.
  </action>
  <verify>cd backend && ! grep -q 'market_data_demo' CLAUDE.md && grep -q 'Running the App' CLAUDE.md</verify>
  <acceptance_criteria>
    - `grep -c 'market_data_demo' backend/CLAUDE.md` returns 0
    - `grep -c '## Demo' backend/CLAUDE.md` returns 0
    - `grep -c '## Running the App' backend/CLAUDE.md` returns 1
    - `grep -c 'uvicorn app.main:app' backend/CLAUDE.md` returns 1
    - The `## Market Data API`, `## Core Types`, `## SSE Streaming`, `## Seed Data`, and `## Running Tests` sections still exist (grep each).
  </acceptance_criteria>
  <done>backend/CLAUDE.md no longer lies about the demo; documents the new uvicorn entrypoint.</done>
</task>

</tasks>

<verification>
- [ ] `cd backend && uv sync --extra dev` exits 0
- [ ] `cd backend && uv run ruff check app/ tests/` exits 0
- [ ] `cd backend && uv run pytest -q` exits 0 with all market + api tests passing
- [ ] `cd backend && uv run python -c "from app.main import app; assert app.title == 'FinAlly'"` exits 0
- [ ] `curl http://localhost:8000/api/health` (against `uv run uvicorn app.main:app --port 8000`) returns 200 + JSON `{status, source, tickers, cache_version}`
- [ ] `curl -N http://localhost:8000/api/stream/prices` yields a `data:` event within 2 seconds
- [ ] `git status backend/pyproject.toml` shows clean
</verification>

<success_criteria>
- pyproject.toml restored from HEAD; existing test suite green
- `app/main.py` boots via `uvicorn app.main:app`; lifespan starts/stops source cleanly
- `/api/health` returns the agreed JSON shape sourced from `app.state`
- `/api/stream/prices` still serves SSE (existing market router mounted, untouched)
- `app/market/` is unmodified — no diff lands there
- backend/CLAUDE.md no longer references the deleted demo
</success_criteria>

<output>
After completion, create `.planning/phases/01-backend-boot-streaming-hello-world/01-01-SUMMARY.md`
</output>
