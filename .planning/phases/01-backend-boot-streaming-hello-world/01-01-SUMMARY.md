---
phase: 01-backend-boot-streaming-hello-world
plan: 01
subsystem: api
tags: [fastapi, uvicorn, lifespan, sse, pytest, httpx]
requires: []
provides:
  - "FastAPI application root (`app.main:app`) with lifespan-managed PriceCache + MarketDataSource"
  - "`/api/health` endpoint reporting status, source label, ticker count, cache version"
  - "`app/api/` package with aggregator `api_router` for future phase endpoints"
  - "Singleton state on `app.state.price_cache` / `app.state.market_source`"
  - "Backend convention pattern: lifespan-mounted SSE router (untouched market subsystem)"
affects: [phase-2, phase-3, phase-5, phase-6, phase-7, phase-8]
tech-stack:
  added: [httpx (dev-only, for ASGI test client)]
  patterns: [lifespan-owned singletons on app.state, build_api_router() factory, _STATIC_DIR.is_dir() guard]
key-files:
  created:
    - backend/app/main.py
    - backend/app/api/__init__.py
    - backend/app/api/health.py
    - backend/tests/api/__init__.py
    - backend/tests/api/test_health.py
    - backend/tests/api/test_main.py
  modified:
    - backend/pyproject.toml
    - backend/uv.lock
    - backend/CLAUDE.md
key-decisions:
  - "lifespan mounts create_stream_router(cache) inside startup, after cache exists — market subsystem stays untouched"
  - "app.state is the singleton bus; routes read via request.app.state.price_cache"
  - "_source_label maps SimulatorDataSource → 'simulator', MassiveDataSource → 'massive'"
  - "Added httpx>=0.27.0 to dev deps for ASGITransport (was not transitively present)"
patterns-established:
  - "Per-concern router file under app/api/ with module-level `router = APIRouter()` and `@router.get(...)`"
  - "Lifespan-context tests use `async with app.router.lifespan_context(app):` + httpx.AsyncClient(ASGITransport(app=app))"
  - "`autouse` fixture clears MASSIVE_API_KEY to force simulator selection in tests"
requirements-completed: [APP-01, APP-02, APP-03, APP-04, APP-06]
duration: 18min
completed: 2026-05-17
---

# Phase 1, Plan 01: Backend Slice Summary

**FastAPI app entrypoint now boots the existing market subsystem; `/api/health` reports live state; tests pass.**

## Performance

- **Duration:** ~18 min
- **Tasks:** 4 completed
- **Files modified:** 9 created/edited

## Accomplishments

- `uvicorn app.main:app --port 8000` now boots a working backend that serves `/api/health` and `/api/stream/prices` from the existing (untouched) `app/market/` subsystem.
- Lifespan wires the `PriceCache` + `SimulatorDataSource` (10 default tickers from `SEED_PRICES`), mounts the SSE router, publishes singletons to `app.state`, and stops cleanly on shutdown.
- `app/api/` package established as the scaffolding for every future phase's endpoints (Phase 2 watchlist, Phase 3 portfolio, Phase 6 chat, Phase 8 sentiment).
- 5 new tests added (`test_health.py` × 2, `test_main.py` × 3); existing 73 market tests unaffected; ruff clean across `app/` and `tests/`.

## Files Created/Modified

- `backend/app/main.py` — FastAPI factory + lifespan
- `backend/app/api/__init__.py` — aggregator `api_router` + `build_api_router()`
- `backend/app/api/health.py` — `GET /api/health` (status, source, tickers, cache_version)
- `backend/tests/api/__init__.py` — empty package marker
- `backend/tests/api/test_health.py` — payload shape + source-label tests
- `backend/tests/api/test_main.py` — app-factory + lifespan tests
- `backend/pyproject.toml` — added `httpx>=0.27.0` to `[dev]`
- `backend/uv.lock` — regenerated by `uv sync`
- `backend/CLAUDE.md` — removed `## Demo` section; added `## Running the App` with uvicorn command

## Verification

- ✓ `cd backend && uv sync --extra dev` exits 0
- ✓ `cd backend && uv run pytest -q` → 78 passed
- ✓ `cd backend && uv run ruff check app/ tests/` → All checks passed
- ✓ Live smoke test against `uvicorn app.main:app --port 8001`:
  - `GET /api/health` → 200, `{"status":"ok","source":"simulator","tickers":10,"cache_version":30}` (then 60 after one more second)
  - `GET /api/stream/prices` → `text/event-stream` with `retry: 1000` + immediate `data:` frame containing all 10 tickers
- ✓ Lifespan logs "FinAlly backend started with 10 tickers via SimulatorDataSource" on startup

## Notes for Next Plans

- `_STATIC_DIR` mount is intentionally deferred to Plan 01-03 (Dockerfile slice) — backend-only `uvicorn` dev runs do not need it and would fail without the directory present.
- The SSE generator at `/api/stream/prices` is still the only route mounted outside `/api/*`. Plan 01-03 will add the `/` static mount last so it does not shadow `/api/*`.
